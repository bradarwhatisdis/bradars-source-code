name: Build library.so

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK
        run: |
          echo "y" | ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager --install "ndk;24.0.8215888" --sdk_root=${ANDROID_SDK_ROOT}

      - name: Build native library
        run: |
          chmod +x gradlew
          export ANDROID_NDK_HOME=${ANDROID_SDK_ROOT}/ndk/24.0.8215888
          export PATH=${ANDROID_NDK_HOME}:${PATH}
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          echo "ANDROID_NDK_HOME=$ANDROID_NDK_HOME"
          echo "ndk-build location: $(which ndk-build || echo 'not found')"
          ./gradlew :app:buildOnlyLibrary --no-daemon

      - name: Upload library.so
        uses: actions/upload-artifact@v4
        with:
          name: library-arm64-v8a
          path: app/src/main/libs/arm64-v8a/library.so
          if-no-files-found: error

      - name: Inject library.so to APK
        run: |
          sudo bash ./inject.sh

      - name: Upload injected APK
        uses: actions/upload-artifact@v4
        with:
          name: injected-apk
          path: output/injected-final.apk
          if-no-files-found: error